const MODEL='gemini-2.5-flash-lite';
const schema={type:'object',properties:{reply:{type:'string'},nodes:{type:'array',maxItems:6,items:{type:'object',properties:{id:{type:'string'},label:{type:'string'},text:{type:'string'},type:{type:'string'},importance:{type:'number'}},required:['id','label','text','type','importance']}},edges:{type:'array',maxItems:8,items:{type:'object',properties:{from:{type:'string'},to:{type:'string'},relation:{type:'string'}},required:['from','to','relation']}}},required:['reply','nodes','edges']};
const json=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{'content-type':'application/json;charset=utf-8','cache-control':'no-store'}});
export async function onRequestPost({request,env}){if(!env.GEMINI_API_KEY)return json({error:'GEMINI_API_KEY is not configured'},503);try{const b=await request.json();const msgs=Array.isArray(b.messages)?b.messages.slice(-16).filter(x=>x&&typeof x.text==='string').map(x=>`${x.role==='assistant'?'OYDIN':'FOYDALANUVCHI'}: ${x.text.slice(0,1800)}`).join('\n'):'';const prompt=`Sen Oydin — inson bilan tabiiy fikrlash suhbati olib boradigan AI hamrohisan. Oydin ChatGPT kloni emas. Sen avval tinglaysan, keyin fikrni tushunasan. Har bir xabarga savol berish shart emas. Ba'zida aks ettir, ba'zida fikrni rivojlantir, ba'zida qarama-qarshilikni muloyim ko‘rsat, ba'zida aniq reja ber. Foydalanuvchi nima istayotganini aytmagan bo‘lsa, uni majburlama. Javob tabiiy, qisqa-o‘rta uzunlikda va o‘zbek tilida bo‘lsin. Psixologik tashxis yoki ortiqcha motivatsiya bermagin.\n\nIchki maqsad: suhbat davomida faqat muhim va ishonchli fikrlarni thinking graph sifatida ajrat. Har bir oddiy gapni node qilma. Faqat yangi yoki muhim signal, maqsad, to‘siq, qaror, insight yoki actionni qo‘sh. Edges faqat real mantiqiy aloqani ko‘rsatsin. Oldingi node bilan bir xil narsani qayta yaratma.\n\nSuhbat:\n${msgs}`;const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${encodeURIComponent(env.GEMINI_API_KEY)}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],generationConfig:{temperature:.7,maxOutputTokens:650,responseMimeType:'application/json',responseSchema:schema}})});const raw=await r.text();let d={};try{d=JSON.parse(raw)}catch{}if(!r.ok)throw Error(`Gemini ${r.status}`);const text=d?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('');if(!text)throw Error('empty');return json({...JSON.parse(text),model:MODEL})}catch(e){console.error(e);return json({error:'AI request failed'},502)}}
